 pipeline {
       agent any
              stages {
                   stage('Clone') {
                         steps {
                            git credentialsId: 'Github', url: 'https://github.com/skillrary/welcometoskillrary.git'
                            }
                         }
                   stage('Build') {
                            steps {
                                sh "mvn clean install"   
                            }
                         }
		     stage ('Server'){
            steps {
               rtServer (
                 id: "jfroglocal",
                 url: 'http://3.143.236.81:8082//artifactory',
                 username: 'admin',
                  password: 'Kumar@12345',
                  bypassProxy: true,
                   timeout: 300
                        )
            }
        }
        stage('Upload'){
            steps{
                rtUpload (
                 serverId:"server" ,
                  spec: '''{
                   "files": [
                      {
                      "pattern": "*.war",
                      "target": "myrepo"
                      }
                            ]
                           }''',
                        )
            }
        }
        stage ('Publish build info') {
            steps {
                rtPublishBuildInfo (
                    serverId: "server"
                )
            }
        }
		      
	          }
     }
